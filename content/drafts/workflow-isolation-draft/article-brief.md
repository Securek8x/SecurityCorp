# Unapproved-draft isolation fixture

This intentionally incomplete, sanitized fixture proves that a directory in
`content/drafts/` does not generate `/knowledge/workflow-isolation-draft/`.
It is not an article proposal and must never be added to the content registry.
